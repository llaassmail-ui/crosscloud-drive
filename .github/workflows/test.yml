name: test

on:
  push:
  pull_request:

jobs:
  powershell:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run release readiness checks
        shell: powershell
        run: .\scripts\Test-ReleaseReadiness.ps1
